apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: argocd-demo
spec:
  replicas: 4
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: nginx:this-image-does-not-exist
          ports:
            - containerPort: 80