apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-prod
  namespace: default
spec:
  project: default

  sources:
    - repoURL: https://github.com/itech0306/argocd-practice
      targetRevision: main
      path: nginx-helm
      helm:
        valueFiles:
          - $values/environments/prod/values.yaml

    - repoURL: https://github.com/itech0306/argocd-practice
      targetRevision: main
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd-prod

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true